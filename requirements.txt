# 依存はpyproject.tomlが正。これは `pip install -r` 用のミラー。
# 開発環境では `pip install -e ".[dev]"` を推奨。
fastapi>=0.115
uvicorn>=0.30
watchfiles>=0.21
pydantic>=2.7
pydantic-settings>=2.3
sqlmodel>=0.0.21
anthropic>=0.40
tweepy>=4.14
APScheduler>=3.10
httpx>=0.27
python-multipart>=0.0.9
typer>=0.12
